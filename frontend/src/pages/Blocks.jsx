import React from 'react';
import BlockExplorer from '../components/BlockExplorer';

const Blocks = () => {
    return (
        <div>
            <BlockExplorer />
        </div>
    );
};

export default Blocks;